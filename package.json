{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "postbuild": "powershell Compress-Archive -Path public/build -DestinationPath public/build.zip -Force",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.1.13",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "vite": "^7.0.7"
    }
}
